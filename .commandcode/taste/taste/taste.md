# Taste
- Communicates in Indonesian (Bahasa Indonesia) and expects responses in the same language. Confidence: 0.9
- When asking a question, wants a direct answer first ("jawab dulu") — the assistant should answer before proposing or doing any implementation, and follow-up offers should come after the answer. For quick factual/clarification questions explicitly asks for brevity ("jawab singkat") — a concise direct reply, not a long structured report. Confidence: 0.7
- Specifies task requirements in project documentation (e.g., a PRD markdown file in `docs/`) and expects the assistant to read the doc first and implement exactly what it defines. Confidence: 0.7
- Prefers project-local configuration for secrets (e.g., API keys) via a git-ignored `.env` file with a committed `.env.example` template, rather than setting environment variables in the terminal each run. Confidence: 0.8
- Gives brief go-aheads and terse directives ("ya coba lanjutkan", "pakai gemini api key", "coba proses lagi", "cek lagi") and expects the assistant to autonomously execute the full multi-phase pipeline (running real API-backed jobs, monitoring long-running background work, and reporting back) once the setup is ready, rather than asking for confirmation at every step. A bare one-word "status" ping means: immediately check the current state of the in-flight job (e.g., `kaggle kernels status`) and reply with a concise status — current state, what will happen next autonomously, and any open decision awaiting the user — not a full report. Confidence: 0.9
- Prefers cost-effective / free-tier solutions when available: when a paid service (OpenAI) hit a billing/quota wall, chose the free-tier provider (Gemini) over fixing billing or paying. Confidence: 0.5
- Values academic rigor in their thesis project: asks explicitly whether methods/metrics/data meet scientific standards in computer science and data science (e.g., annotation quality via Cohen's Kappa, Krippendorff's Alpha, F1 against human ground truth), and expects the assistant to cite established literature plus industry practice and distinguish what is standard vs. missing. Confidence: 0.85
- Wants data quality assurance before proceeding: explicitly asks to confirm the dataset is the best it can be and problem-free, and expects the assistant to ground quality criteria/checklists in scientific literature and industry practice and to compare the actual data condition against those targets rather than just assert quality. Repeatedly re-checks after each round of work ("cek apakah standar sudah memenuhi?") rather than taking a previous verification as final. Confidence: 0.8
- Edits data CSVs in Excel on Windows, which saves files with Windows-1252 encoding and semicolon (`;`) delimiters instead of UTF-8/comma; shared CSV-loading code should auto-detect encoding and delimiter so Excel-saved files are read correctly. Confidence: 0.8
- Asks for written guides ("bisa berikan guide bagi saya?", "buat panduan ... dari cli") and expects the guide to be delivered as a reusable markdown file in the project `docs/` folder — with quick-reference commands per phase, configuration, from-scratch workflow, troubleshooting table, and output locations — not just a chat explanation. Confidence: 0.8
- Works on Windows (local laptop) as the primary environment; guides and commands should use Windows syntax (cmd/PowerShell, `copy`, `where`, `Test-Path`, `%USERPROFILE%`, `D:\` paths) instead of Unix equivalents. Confidence: 0.7
- Prefers running Kaggle cloud-GPU training from the local CLI (`kaggle datasets version/create`, `kaggle kernels push`, `kaggle kernels status/output/pull`) driven by `kernel-metadata.json` + `dataset-metadata.json` configs, rather than uploading notebooks/data via the browser. Reconfirmed explicitly when setting up the LoRA notebook: the user stated they only want to run the push command from the CLI. Confidence: 0.8
- When asking how to do something ("secara step by step apa yang harus saya eksekusi di cli?", "bagaimana langkahnya?"), wants an explicit, ordered sequence of exact commands plus the exact files involved (what to upload and from which folder) — not a high-level conceptual walkthrough. Confidence: 0.75
- Insists on verifying the evaluation pipeline and reported metrics (e.g., cross-checking a reported validation Macro F1 against the confusion matrix) before any tuning or enhancement; treats metric inconsistencies as P0 that must be root-caused first, because wrong validation metrics bias every subsequent tuning decision. Confidence: 0.9
- Prefers diagnosing the real model bottleneck from evidence (confusion matrix, class distribution, per-class precision/recall/F1) before choosing enhancement techniques, and rejects generic fixes not justified by the diagnosis (e.g., explicitly advises against SMOTE/oversampling when imbalance is only moderate and the actual problem is an ambiguous class). Confidence: 0.8
- For suspicious metrics, expects the assistant to trace them to the source: when a figure in a notebook comparison table didn't match other cells, traced it back by inspecting the notebook cells' source code — proving the numbers were hardcoded/placeholder values in a template (with comments like `# sesuaikan hasil empiris`, `# ganti sesuai hasil aktual`) rather than computed outputs — and used that evidence to conclude the table must not be cited. When a discrepancy is flagged, explicitly asks for the difference and its root cause ("jelaskan apa perbedaanya, mengapa berbeda") — wants each number traced to where it was computed vs. hardcoded, not just a restatement. Confidence: 0.85
- Wants experiment stages compared with rigorous metrics and significance testing — Macro F1, Weighted F1, per-class recall (especially the weak class), Expected Calibration Error (ECE), and McNemar test — not accuracy alone. Confidence: 0.7
- Wants evaluation/verification tooling built as reusable Python modules in `quality_pipeline/` following that package's conventions (Indonesian docstrings, `run(dry_run=...)` + argparse `main()`, import from `quality_pipeline.config`/`utils`), with usage guides as markdown in `docs/` — explicitly chose this over a new standalone verification notebook or injecting extra cells into existing notebooks (notebooks stay as run artifacts; `04_model_indobertweet_lora.ipynb` is canonical). Confidence: 0.85
- Script/CLI output must be ASCII-safe (use `-` instead of em-dash, `!` instead of ⚠): the Windows console is cp1252 and non-ASCII characters garbled in real runs of `quality_pipeline/verify_metrics.py`, requiring a fix. This also bites third-party CLIs: `kaggle kernels output` can crash with UnicodeEncodeError when printing a kernel log containing unicode — workaround via `set PYTHONIOENCODING=utf-8` / `PYTHONUTF8=1`, or bypass the CLI entirely and pull through the Python API (`KaggleApi().authenticate(); api.kernels_output(slug, path=...)`). Confidence: 0.8
- Prefers tightly scoped work packages: accepted P0 as "verify pipeline + fix evaluation bugs only" and deferred retraining the collapsed LSTM baseline to a separate follow-up step, rather than bundling training fixes into the metric-verification phase. Confidence: 0.6
- Wants a full handoff boundary on setup tasks: the assistant should perform the entire preparation end-to-end (creating kernel folders, fixing/linearizing notebooks, writing `kernel-metadata.json`, validating everything, checking CLI availability) so the user's only remaining action is executing the final command(s) themselves (e.g., `kaggle kernels push -p temp_kernel_lora`). Explicitly requested: "lakukan setup hingga akhirnya saya tinggal melakukan command untuk membuka notebook ke kaggle dari cli saja." Confidence: 0.9
- Evaluates plans against explicit pass/fail criteria: asks "apa kriteria gagal dari rencana ini?" and defines success as reaching a concrete quantitative performance threshold (≥80% performance on the thesis model). Expects a plan to state its failure gates upfront and to pre-plan the next option if the target is missed (e.g., "jika masih gagal pertimbangkan opsi selanjutnya ... menangani imbalanced?") rather than improvising contingency after the fact. Confidence: 0.7
- When handing the project off to someone else, wants a single consolidated handover document in `docs/` (e.g., `docs/HANDOVER.md`) written for an external reader — covering project summary, evolution chronology of work phases with traces in the repo, a deliverables inventory (location + how to use each), current status & open agenda, quick-start commands (Windows), an index of all documentation, and working principles to preserve — not just a chat summary. Confidence: 0.8
- Wants zero-touch Kaggle automation: explicitly asked to "connect" the assistant to Kaggle so they never have to go back and forth manually — the assistant should run the full cloud-run lifecycle itself (poll `kaggle kernels status`, auto-wake on COMPLETE/ERROR, pull outputs/logs, diagnose failures, fix notebooks, push new kernel versions, re-monitor, then verify and report). This extends the earlier setup-handoff boundary (where the user's only action was the final `kaggle kernels push`) to full agent-run monitoring and repair. Confidence: 0.85
- Environment fact enabling the above: the Kaggle CLI (v2.2.4) with valid credentials is installed on the local Windows machine, so `kaggle kernels status/output/push` can be executed directly from the assistant's shell without any user involvement. Confidence: 0.9
- For long-running Kaggle jobs, keep the assistant in the loop via a background monitor with scheduled notify that polls `kaggle kernels status` every ~3 min until COMPLETE/ERROR, so the assistant wakes on completion/error and continues the pipeline (pull → verify → report) autonomously. Implementation lessons from practice: a Python `time.sleep` poller ran silent for ~25 min producing no output — a cmd `.bat` loop appending status to a tail file and grepping it proved more reliable; and inside such a .bat (no console stdin) `timeout /t` fails, so use `ping -n N 127.0.0.1 >nul` for the delay; even so, a monitor once vanished from the task list without waking the assistant and a ~45-min silent gap prompted the user to ping "kenapa stuck?" — when that happens, check `kaggle kernels status` directly and triage (run state + monitor state) rather than assuming the monitor is alive. Confidence: 0.75
- When a pushed Kaggle notebook fails, apply the fix to both maintained copies — canonical `results/*.ipynb` and the Kaggle push copy (`temp_kernel_lora/*.ipynb`) — validate with nbformat (asserting the edited pattern occurs exactly once) before re-pushing, and record the new failure mode in the troubleshooting table of the relevant `docs/` plan (reconfirmed on the torchao, single-GPU, and dataset-mount-path fixes). Confidence: 0.8
- Kaggle kernel runs should use GPU T4 x2 with internet on: the user explicitly requested this accelerator setting ("internet on dan accelerator: gpu t4 x2?"); implemented as `machine_shape: "NvidiaTeslaT4"` + `enable_internet: true` set permanently in `temp_kernel_lora/kernel-metadata.json` so every future push inherits it. Confidence: 0.8
- For documentation-only Kaggle kernels (data augmentation/preprocessing pipelines, no model training — e.g., the SMOTE+LLM augmentation notebook), GPU is turned off in `kernel-metadata.json` (`enable_gpu: false`, no `machine_shape`) to conserve the weekly T4 quota for training runs, while `enable_internet: true` stays on because the kernel needs to call the LLM API and install packages like `google-genai`. Confidence: 0.55
- On Kaggle, avoid the P100 accelerator: the default PyTorch image has no Pascal (sm_60) kernels, so `torch.cuda.is_available()` reports True but the first CUDA op fails with `AcceleratorError: cudaErrorNoKernelImageForDevice`. Valid `machine_shape` values in `kernel-metadata.json` (CLI 2.x): `NvidiaTeslaT4` (= "GPU T4 x2" in the UI, preferred), `NvidiaTeslaP100` (avoid), `Tpu1VmV38`. Confidence: 0.85
- Kaggle CLI `kernels push` executes immediately and validates nothing client-side: an invalid `machine_shape`/`--accelerator` value is accepted at push time and only fails at runtime (burned a kernel version). Never probe state-changing push commands with dummy values — determine valid enum values from the installed package source / official metadata docs first. Confidence: 0.8
- Wants to inspect raw artifacts directly: when a result or discrepancy is being discussed, asks for the exact file locations ("dimana file terkait saya ingin membacanya langsung") so they can open and read the actual files themselves — the assistant should give precise paths for both local artifacts (e.g., `results/*.ipynb`) and remote Kaggle outputs (CSVs in `/kaggle/working/`), plus the pull command to fetch them into the repo, not just a chat explanation. Confidence: 0.7
- Wants provenance/traceability for reported results: when a "best model" or a headline metric is mentioned, asks where it comes from ("dari mana best model ini berasal?") and expects the answer to trace it back through the actual experiment artifacts — which tuning trial/notebook cell produced it, exact hyperparameters, and the selection criterion (e.g., grid-search trial 4 chosen by highest validation f1_macro, then retrained) — not just a restatement of the number. Also asks whether the claimed best actually matches the recorded best ("apakah best model sudah sesuai dengan hasil terbaik?"), i.e., expects the hardcoded `best_params`/config constants to be cross-checked against the actual tuning outputs and any internal inconsistency between notebook cells to be flagged rather than glossed over. Confidence: 0.8
- Uses the project's `docs/` progress documents (e.g., `docs/HANDOVER.md`, `docs/P0_VERIFIKASI_EVALUASI.md`, `docs/PLAN_PASCA_RERUN.md`) as the reporting artifact to their supervisor: when new experiment results land, asks to have the latest details added into those existing docs ("di dokumen progress tambahkan detail hasil terbaru ini") rather than only summarized in chat — expecting stale status/numbers in all affected docs to be updated consistently, with unverified results clearly marked as not-yet-final. Confidence: 0.7
- Demands replicability ("prinsip replicability") as a hard principle for all experiment work — explicitly "selalu mengedepankan prinsip replicability: apa yang kamu hasilkan harus bisa saya koreksi dan ulangi sendiri". Operationalized as: fixed seed (42), an identity cell printing python/torch/transformers/peft versions in every experiment notebook, methods/formulas written out explicitly (e.g. custom loss, coordinate-ascent), per-sample prediction+probability CSVs saved for BOTH the validation and test splits (fresh lesson from E1: test probs were saved but validation probs were not, which blocked offline E3 threshold calibration and forced a methodologically weaker calibration/holdout split of the test set — always save softmax probs at eval time for every split), and numbers accepted only when reproduced by the `verify_metrics.py` harness. Confidence: 0.9
- Wants a strict pre/post experiment-logging contract around every run: BEFORE pushing — current condition (numbers + harness result), exactly what changes (precise parameters/cells), written expectations (hypothesis + target numbers + how measured) and risks; after the run — process (including failures/bottlenecks and how they were handled), findings (including negative results), results (table + gate verdict), and next steps. Implemented as `docs/LOG_EKSPERIMEN.md` with PRA/PROSES/PASCA sections, pre-registered before each push. Confidence: 0.9
- Requires all experiment artifacts kept complete per the original repo conventions — source notebook (numbered, root), executed result notebook pulled into `results/`, and final result CSVs committed — so the final all-methods comparison stays whole ("jangan lupa menyimpan notebook source code, notebook hasil, dan hasil akhir sesuai repo awal yang membandingkan seluruh metode"). Confidence: 0.8
- Treats development as iterative, threshold-gated experimentation with explicit freedom to deviate: "kamu bebas melakukan apapun eksperimen selama threshold tadi tercapai" — plan order is priority, not dogma; any experiment is fair game if it follows the replication protocol, has a written hypothesis, and harness-verified numbers; approaches that exhaust their iteration budget without passing gates are recorded as negative findings and the plan pivots instead of forcing them. Confidence: 0.85
- On Windows cmd, multi-line `python -c` one-liners repeatedly break on quoting ("lagi bermasalah... pelajarannya konsisten: pakai file skrip") — write a scratchpad .py file and run it instead, even for small test-data generation. Confidence: 0.8
- Before pushing any new experiment notebook to Kaggle, run a full validation pass: `nbformat.validate`, compile every code cell (skipping `!` magic lines), assert required content markers are present (explicit `text_bert` column, sanity-check/collapse check, prob columns), and check kernel-metadata (machine_shape, internet, code_file). Smoke-test new reusable tools on synthetic data before touching real data (McNemar on synthetic CSVs; threshold calibration on a synthetic anti-neutral-biased model). Confidence: 0.7
- Prefers single-GPU protocol fidelity over multi-GPU throughput on Kaggle: with T4 x2, HF Trainer wraps the model in `DataParallel` — effective batch doubles (16→32), optimizer updates halve (975 vs 1950 steps), the model undertrains (train loss stuck ~1.35 vs 0.64 canonical) and custom Trainers crash (`'DataParallel' object has no attribute 'config'`). Fix pattern: the first code cell of every training notebook sets `os.environ['CUDA_VISIBLE_DEVICES'] = '0'` so the run protocol exactly matches the canonical 1-GPU run (hardware stays T4 x2; training executes single-GPU), applied across all maintained notebook copies. Confidence: 0.85
- Diagnoses unexpected metric degradation from run forensics, not guesses: reads `trainer_state.json` (train_batch_size, global_step vs expected step count, per-epoch train loss) and the kernel log's final entries, compares against the canonical run's profile, and only then states the root cause. Confidence: 0.85
- When a verification gate fails on numbers (e.g., Macro F1 0.6360 vs 0.7328, Δ ≫ 0.02 tolerance), the baseline stays unlocked and the finding is recorded as partial (Δ table + root cause + action) in the experiment log; to save GPU time, merges the replication check into the next planned run instead of burning a separate full re-run (E1's ε=0 training arm doubles as the single-GPU baseline replication — one run confirms the root cause, locks E0, and delivers the E1 sweep). Confidence: 0.75
- Wants a plain-language accounting after autonomous work stretches: after long agent-driven periods the user asks pointed meta-questions ("apa yang kamu periksa?", "apa tugas dari buat docs/log_experimen.md?", "kenapa sangat lama?", "apa yang sudah terjadi?") — expects brief explanations of what exactly was inspected/done, the purpose and value of each documentation artifact created (e.g., why the pre/post experiment log exists), and an honest account of where the time goes (each cloud-GPU run cycle ~40–60 min, slow output downloads, retry loops), not silence or a bare "done" report. Recurred when the user returned mid-run asking "apa yang sudah terjadi?" and was best served by a structured recap table (per-version results + lessons + current status). Confidence: 0.7
- Distinguishes transient Kaggle failures from code bugs before touching notebooks: when a freshly pushed kernel dies almost immediately (E1 v1 failed in 43 s with FileNotFoundError on /kaggle/input/...csv), check the pushed `kernel-metadata.json` (`kaggle kernels pull -m`) and `kaggle datasets status` — if both healthy and the crash is an early missing-mount error, first suspect a transient mount race and re-push; but if the identical failure repeats (E1 v2 crashed the same way), reclassify as systematic and escalate to instrumented diagnosis instead of retrying again. Confidence: 0.7
- Debugs repeated failures by instrumenting the environment, not by guessing: when E1 failed identically twice, the notebook was rebuilt with a self-diagnosing data-load cell (`os.walk("/kaggle/input")` printing every mounted file, plus an explicit `raise FileNotFoundError` that lists what IS mounted vs what was expected) and a version was pushed whose primary purpose was evidence-gathering; the fix (P0.2) only landed after the diagnostics revealed the real mount layout. Failure messages should state expected vs found, not just the exception. Confidence: 0.8
- Kaggle CLI 2.x pushes mount datasets under a new path schema — `/kaggle/input/datasets/<owner>/<slug>/` — while older UI-attached kernels keep the legacy `/kaggle/input/<slug>/`; hardcoding `DATASET_DIR` to the legacy path silently misses even though the dataset is mounted and `dataset_sources` is correct. Fix pattern: never hardcode dataset paths in pushed notebooks — resolve input files from the mount listing (`next(p for p in mounted if p.endswith("data_preprocessed_with_emoticon.csv"))`) so the notebook works under both schemas; schema difference recorded in the troubleshooting table. Confidence: 0.8
- Live progress on a RUNNING Kaggle kernel is obtainable without waiting for completion: `kaggle kernels output <owner>/<slug> -p <dir>` works mid-run and fetches the current in-progress log, which can then be tail-read (via the scratchpad `read_kaggle_log.py`) to confirm the run passed a previously failing stage or is actually training. Use it whenever a monitor's aliveness is uncertain or the user asks what is happening right now — don't just report the coarse `kernels status` state. Confidence: 0.7
- Kaggle `kernels status` can report stale state (still RUNNING hours after the kernel actually completed); don't trust it as the sole liveness signal — also try pulling outputs (`kaggle kernels output` or Python API) to check if result files already exist, which proves completion. When the user reports an implausibly long runtime (e.g., 24 h vs the 9 h Kaggle maximum), suspect status staleness and pull outputs immediately rather than assuming the run is genuinely still executing. Confidence: 0.8
- When reproducing or comparing against a canonical baseline run, pin the library stack to the canonical run's era instead of accepting the Kaggle image's latest versions: E1 v4 ran on transformers 5.0.0 / torch 2.10.0 / peft 0.19.1 and scored val macro F1 0.655 vs canonical 0.723 on identical data and settings, so v5 pinned `transformers==4.46.3` + `peft==0.13.2` as the decisive single-variable test. Major-version drift (e.g., transformers 5.x vs the canonical 4.x era) is treated as a first-class suspect for metric gaps, and the resolved stack of every run is recorded for replication. Confidence: 0.85
- Verify data lineage before attributing performance gaps to the model: compare MD5 hashes of the local canonical CSV vs the version uploaded to Kaggle, compare label distributions against the canonical run's recorded splits, and cross-check pickle vs CSV split reconstruction (sizes, label distribution, per-row text/label identity, first-differing-row examples). This caught that the Kaggle dataset still held the OLD labels (4843/1465/2340 = canonical run `04`) while local `Data/` already held NEW corrected labels (4686/1510/2452) — uploaded cloud datasets can silently go stale relative to local data, which affects whether runs are comparable. Confidence: 0.8
- When several suspects remain after a failed replication, resolve the cheap, no-GPU ones locally first (hash comparisons, pickle-vs-CSV lineage check, grepping the kernel log for label distributions / step counts) and spend GPU reruns only on the one remaining decisive suspect, one variable at a time. Confidence: 0.7
- When a finding raises a research-level (not technical) decision — e.g., keep the old labels for comparability with all existing numbers vs switch to the corrected labels and recompute every baseline — do not choose unilaterally: present explicit options (a)/(b) with their academic and practical consequences and ask the user to decide, timing the question to when the pending run finishes so the answer can steer the next experiment. Confidence: 0.85
- Works asynchronously on long-running GPU work and expects seamless pause/resume: explicitly asked to continue the work later ("bisakah dilajutkan nanti?") rather than keep the session open — the assistant should leave the state durably resumable (experiment log with PRA/PROSES/PASCA up to date, kernel version/status and remaining decisions recorded, queued Kaggle runs left to complete unattended) and confirm exactly how to resume ("tinggal bilang 'lanjutkan' atau 'status'"). Recurred in a second session; the user resumes with bare "status"/"lanjutkan" and expects the assistant to pick up the thread without any re-briefing. Confidence: 0.75
- Mixed-environment failures are fixed at the source with version pins, not by re-running and hoping: when a kernel/notebook run fails because of stack mixing (e.g., transformers 5.0 files left over alongside a 4.x pin), the fix is to pin the stack explicitly in the build script (exact versions for transformers/peft/tokenizers/huggingface-hub), add a version-identity + assert guard cell that fails fast on any mixed install, and re-push — not to retry the same command. Confidence: 0.85
- Version-print/identity cells go AFTER the pin step and must carry an assert guard: the run-identity cell (printing python/torch/transformers/peft versions) is placed at the end of the build so it prints the actually-installed post-pin stack, and its output is asserted against the expected pinned versions so a silently mixed environment is caught immediately instead of producing a half-comparable run. Confidence: 0.8
- Remediating a failed run starts with forensics on the log, not a blind retry: before re-pushing, read the pulled kernel log (which stage failed, expected-vs-found), classify the failure as transient (mount race — just re-push) vs systematic (mixed stack — pin + guard), and only then apply the matching fix pattern. Confidence: 0.8
- Resolves research-level data decisions in favor of ground-truth quality: when offered the choice between keeping the old labels (all existing numbers stay comparable) and switching to the corrected annotation-pipeline labels (4686/1510/2452) with full baseline recomputation, chose the corrected labels — ground-truth correctness ranks above historical comparability, and the migration was executed (Kaggle dataset versioned with the corrected CSV, `kaggle_dataset/split_data.pkl` regenerated to the new labels). Confidence: 0.8
- Pulling Kaggle kernel outputs into a directory that already holds previous files silently yields stale data: the API skips files that already exist locally (pulled CSVs still contained the old v7 results with old timestamps even though v8 was COMPLETE). Fix pattern: pull each kernel version's outputs into its own fresh, per-version directory (e.g., `.kaggle-outputs/e1v8/`) and sanity-check the downloaded files' timestamps/content before reading results — never reuse an output directory across versions. Confidence: 0.8
